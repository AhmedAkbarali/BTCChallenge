<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:150' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="form1" method="post">
          <input name="address" type="text" class="ghost-input" placeholder="Enter a Bitcoin address" required>
          <input type="submit" class="ghost-button" value="Get Balance">
        </form>
        <form action="form2" method="post">
          <input name="sender" type="text" class="ghost-input" placeholder="Enter a sending Bitcoin address" required>
          <input name="recipient" type="text" class="ghost-input" placeholder="Enter recipient Bitcoin address" required>
          <input name="amount" type="text" class="ghost-input" placeholder="Enter Amount" required>
          <input type="submit" class="ghost-button" value="Make payment">
        </form>
        <% if(locals.balance !== null){ %>
          <p><%= locals.balance %></p>
        <% } %>

        <% if(locals.error !== null){ %>
          <p><%= locals.error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
